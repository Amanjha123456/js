

let index = 0
while (index <= 10) {
    //console.log(`value of index is ${index}`);
    index = index + 2

    
}
let arr = 0
let myarray = ['flash','batman','superman']
while (arr < myarray.length) {
    //console.log(`value of array index is ${myarray[arr]}`);
    arr = arr + 1
}
let score = 11

 do  {
    console.log(`score is ${score}`);
    score++
 }
 while (score <=10) {
    //score ++ // isme pehle kaam hota hai phir condition check hota hai
    
    
 }